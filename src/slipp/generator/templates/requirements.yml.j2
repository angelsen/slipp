# Generated by slipp launch
# Installed automatically by `slipp deploy`
collections:
  - name: ansible.posix
{% if container_runtime == 'podman' %}
  - name: containers.podman
{% else %}
  - name: community.docker
{% endif %}
